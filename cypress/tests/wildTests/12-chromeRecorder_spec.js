describe("Chrome Recorder to generate spec", () => {
  it(" https://github.com/cypress-io/cypress-chrome-recorder ", () => {
    expect(true).to.equal(true);
  });
});
// npx @cypress/chrome-recorder demo.json --output=cypress/tests
